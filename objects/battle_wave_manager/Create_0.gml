live;
enabled = false
wave = -1
waves = ds_queue_create()
wave_inst = noone

Anim_Create(id,"enabled",0,0,0,1,0,60)

ds_queue_enqueue(waves,to0)
ds_queue_enqueue(waves,to0)
ds_queue_enqueue(waves,to0)
ds_queue_enqueue(waves,to0)
ds_queue_enqueue(waves,to0)